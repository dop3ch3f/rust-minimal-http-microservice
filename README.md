# Rust Hyper Minimal HTTP microservice

- created with the hyper crate

# Run

- setup rust
- cargo run or cargo-watch -x "run"

# Features

- basic CRUD
- basic routing and method matching
- in memory storage